{
  "name": "reviewapp",
  "private": true,
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "npm install --prefix api && npm install --prefix frontend && npm run build --prefix frontend",
    "start": "npm run build && npm run start-backend",
    "start-backend": "npm run start --prefix api",
    "preview": "vite preview",
    "lint": "eslint . --ext js,jsx,ts,tsx --report-unused-disable-directives --max-warnings 0"
  },
  "dependencies": {
    "concurrently": "^7.6.0"
  }
}
